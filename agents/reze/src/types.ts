// Re-export event and domain types from shared package.
export type {
  RezeEvent,
  CoralWebSocketEvent,
  CoralEventType,
} from "@rezeclaw/coral-types/events";

export type {
  CoralThread,
  CoralMessage,
  CoralAgent,
  CoralSession,
} from "@rezeclaw/coral-types/domain";
